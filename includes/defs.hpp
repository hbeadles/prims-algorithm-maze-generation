#pragma once

/**
 * @def SCREEN_HEIGHT - The height of the application window in pixels
 * @def SCREEN_WIDTH - The width of the application window in pixels
 *
 * @brief These are defaults that are overridden by the Application struct when resizing
 */
#define SCREEN_HEIGHT 800
#define SCREEN_WIDTH 800